services:
  redis:
    image: redis:7-alpine
    container_name: aigo-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - aigo-redis-data:/data
    command: ["redis-server", "--appendonly", "yes"]

volumes:
  aigo-redis-data:
